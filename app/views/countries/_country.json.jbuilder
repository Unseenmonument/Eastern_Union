json.extract! country, :id, :name, :mim_cost, :nextday_cost, :exchange_rate, :require_id, :require_testq, :communist_q, :req_twoid, :created_at, :updated_at
json.url country_url(country, format: :json)
